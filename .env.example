# Clé API Anthropic — utilisée côté serveur par /api/ai
# Pour un déploiement (Vercel, etc.), définissez cette variable d'environnement.
# Sinon, l'app permet de coller une clé directement dans l'UI (bouton Réglages).
ANTHROPIC_API_KEY=sk-ant-...
